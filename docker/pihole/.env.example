# Pi-hole admin web UI password.
# Only applied on FIRST container boot; subsequent boots keep the value
# already stored in /root/pihole/etc-pihole. To rotate after first setup:
#   pihole -a -p '<new-password>' (from inside the container)
WEBPASSWORD=change-me
