services:
  node-exporter:
    # Digest pin (no version label upstream). Update by pulling by tag,
    # then replace the digest with docker image inspect --format '{{.RepoDigests}}'.
    image: prom/node-exporter@sha256:1b4e4438faca4dd7e001dd445d161a4a2091b0fededa84093b3a8dfeae1f1be0
    container_name: node-exporter
    restart: unless-stopped
    ports:
      - "9100:9100"
